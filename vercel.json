{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "methods": ["GET", "POST", "PATCH", "DELETE", "LISTEN"],
      "dest": "src/index.ts"
    }
  ]
}
